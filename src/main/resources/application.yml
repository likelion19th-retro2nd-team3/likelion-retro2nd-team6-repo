spring:
  application:
    name: likelion-retro2nd-team3-repo

  datasource:
    url: jdbc:mysql://localhost:3306/librarydb
    username: spring
    password: spring1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true